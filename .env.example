# RunOS environment — all values are public client keys protected by RLS / project settings.
# Defaults are baked into the code for the pilot; set these to override per environment.

# Supabase (live mode: accounts, clubs, events, RSVPs)
NEXT_PUBLIC_SUPABASE_URL=https://srujvjjncrszhaaxepxf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key from Supabase dashboard>

# PostHog (funnel analytics)
NEXT_PUBLIC_POSTHOG_KEY=<posthog project key>
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com
